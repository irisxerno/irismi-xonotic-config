bind TAB "// not bound"
bind ENTER "// not bound"
bind ESCAPE "togglemenu"
bind SPACE "// not bound"
bind 0 "// not bound"
bind 1 "// not bound"
bind 2 "// not bound"
bind 3 "// not bound"
bind 4 "// not bound"
bind 5 "// not bound"
bind 6 "// not bound"
bind 7 "// not bound"
bind 8 "// not bound"
bind 9 "// not bound"
bind BACKQUOTE "toggleconsole"
bind a "// not bound"
bind b "// not bound"
bind c "// not bound"
bind d "// not bound"
bind e "// not bound"
bind f "// not bound"
bind g "// not bound"
bind h "// not bound"
bind i "// not bound"
bind j "// not bound"
bind k "// not bound"
bind m "// not bound"
bind q "// not bound"
bind r "// not bound"
bind s "// not bound"
bind t "// not bound"
bind u "// not bound"
bind v "// not bound"
bind w "// not bound"
bind x "// not bound"
bind y "// not bound"
bind z "// not bound"
bind TILDE "toggleconsole"
bind BACKSPACE "// not bound"
bind UPARROW "// not bound"
bind DOWNARROW "// not bound"
bind LEFTARROW "// not bound"
bind RIGHTARROW "// not bound"
bind SHIFT "// not bound"
bind F1 "// not bound"
bind F2 "// not bound"
bind F3 "// not bound"
bind F4 "// not bound"
bind F5 "// not bound"
bind F6 "// not bound"
bind F7 "// not bound"
bind F8 "// not bound"
bind F9 "// not bound"
bind F10 "// not bound"
bind F11 "// not bound"
bind F12 "// not bound"
bind PAUSE "// not bound"
bind KP_INS "// not bound"
bind KP_END "// not bound"
bind KP_DOWNARROW "// not bound"
bind KP_PGDN "// not bound"
bind KP_LEFTARROW "// not bound"
bind KP_5 "// not bound"
bind KP_RIGHTARROW "// not bound"
bind KP_HOME "// not bound"
bind KP_UPARROW "// not bound"
bind KP_PGUP "// not bound"
bind KP_DEL "// not bound"
bind KP_SLASH "// not bound"
bind KP_MULTIPLY "// not bound"
bind KP_MINUS "// not bound"
bind KP_PLUS "// not bound"
bind KP_ENTER "// not bound"
bind MOUSE1 "// not bound"
bind MOUSE2 "// not bound"
bind MOUSE3 "// not bound"
bind MWHEELUP "// not bound"
bind MWHEELDOWN "// not bound"
bind MOUSE4 "// not bound"
bind MOUSE5 "// not bound"
bind JOY1 "// not bound"
bind JOY2 "// not bound"
bind JOY3 "// not bound"
bind JOY4 "// not bound"
bind JOY5 "// not bound"
bind JOY6 "// not bound"
bind JOY7 "// not bound"
bind JOY8 "// not bound"
bind JOY9 "// not bound"
bind JOY10 "// not bound"
bind JOY11 "// not bound"
bind JOY12 "// not bound"
